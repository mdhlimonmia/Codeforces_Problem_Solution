#include <bits/stdc++.h>
using namespace std;
#define endl '\n'
#define Limon() ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);

void solve(){
    char a,b,c;
    cin>>a>>b>>c;
    if(a == 'a' || b == 'b' || c == 'c')cout<<"YES"<<endl;
    else cout<<"NO"<<endl;
}

int main()
{
    Limon();

    int _;cin>>_;
    while (_--)
    {
	   solve();
    }
    
}